# name: Build

# on:
#   workflow_run:
#     workflows: ['Main']
#     types:
#       - in_progress

# jobs:
#   _pre:
#     name: Variables
#     runs-on: ubuntu-latest
#     env:
#       GH_TOKEN: ${{ github.token }}
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
#       - name: Variables
#         run: |
#           gh variable set -f ${{ inputs.dot-env }}
#           gh variable list
#           env

  # build:
  # test:
  # scan:
  # save_scan: